package ibatis.services.user.impl;

import java.io.Reader;
import java.util.List;

import org.apache.ibatis.io.Resources;
import org.apache.ibatis.session.SqlSession;
import org.apache.ibatis.session.SqlSessionFactory;
import org.apache.ibatis.session.SqlSessionFactoryBuilder;

import ibatis.services.domain.User;
import ibatis.services.user.UserDAO;
import ibatis.services.user.test.TestUtil;

// DAO와 batis를 연결하기 위해 MyBatisUserDaoImpl이 SqlSession을 가져와야 함.

public class MyBatisUserDAOImpl10 implements UserDAO{
	private SqlSession sqlSession;
	
	public void setSqlsession(SqlSession sqlSession) {
		this.sqlSession = sqlSession;
		System.out.println("::"+getClass().getName()+".setSqlSession() Call...");

	}

	@Override
	public int addUser(User user) {
		int result=sqlSession.insert("UserMapper10.addUser",user);
		return 0;
	}

	@Override
	public int updateUser(User user) {
		int result=sqlSession.update("UserMapper10.updateUser",user);
		return 0;
	}

	@Override
	public int removeUser(User user) {
		int result=sqlSession.delete("UserMapper10.removeUser",user);
		return 0;
	}

	@Override
	public User getUser(String userId) throws Exception {
		User user=sqlSession.insert("UserMapper10.getUser",userId);
		return null;
	}

	@Override
	public List<User> getUserList(User user) throws Exception {
		
		return null;
	}
	
}
